spring.application.name=prototype

# Supabase PostgreSQL Configuration
# Replace these values with your actual Supabase database credentials
# Database Configuration for Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.dwgwtreoylgpxzetzjkk
spring.datasource.password=M4x1miliano.22.04.87
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Configuration for Supabase
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey12345678901234567890123456789012345678901234567890}
jwt.expiration=86400000

# Server Configuration
server.port=${PORT:8081}

# Logging Configuration
logging.pattern.console=[%thread] %-5level: %logger - %msg%n
logging.level.root=INFO
logging.level.com.conozca.prototype=DEBUG
spring.main.banner-mode=off